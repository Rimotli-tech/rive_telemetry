## 0.1.0

- Add pure-Dart `.riv` metadata inspector.
- Add stable metadata JSON export with `schemaVersion: 1`.
- Add fixture coverage for artboards, animations, state machines, inputs,
  warnings, invalid files, and unknown record traversal.
