name: demo
description: "Flutter web demo for the RiveTelemetry scaffold."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.13.20
  rive_telemetry:
    path: ../package

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
