name: rive_telemetry_example
description: Minimal example for the rive_telemetry package.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.14.2
  rive_telemetry:
    path: ..

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/demo.riv
    - assets/demo_2.riv
