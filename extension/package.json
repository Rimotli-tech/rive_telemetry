{
  "name": "rive-telemetry",
  "displayName": "RiveTelemetry",
  "description": "Developer tooling for inspecting and debugging Rive runtime behavior.",
  "version": "0.2.0",
  "publisher": "local",
  "engines": {
    "vscode": "^1.118.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:riveTelemetry.openPanel"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "riveTelemetry.openPanel",
        "title": "RiveTelemetry: Open Panel"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "check": "tsc --noEmit -p ./"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@types/ws": "^8.5.13",
    "@types/vscode": "^1.118.0",
    "typescript": "^5.9.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Rimotli-tech/rive_telemetry"
  }
}